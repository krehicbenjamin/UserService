apiVersion: v1
kind: Service
metadata:
  name: userservice
  namespace: production
  labels:
    app: userservice
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: userservice

