apiVersion: v1
kind: Secret
metadata:
  name: userservice-secrets
  namespace: production
type: Opaque
stringData:
  database.username: "REPLACE_WITH_VAULT_OR_SEALED_SECRET"
  database.password: "REPLACE_WITH_VAULT_OR_SEALED_SECRET"
  jwt.secret: "REPLACE_WITH_VAULT_OR_SEALED_SECRET"

